<% if session[:user_id].present? %>

   Welcome back, <%= User.find(session[:user_id]).username %>

<% else %>

  <%= link_to 'Sign Up', new_user_path %>
  <%= link_to 'Log In', login_path %>

<% end %>
